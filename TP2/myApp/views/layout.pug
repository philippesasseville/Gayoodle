doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    ul
      each page in [{path: "/",name : "Accueil" }, {path: "/game",name : "Tableau de Bord" }, {path: "/rules",name : "Instructions" }]
        if path === page.path
          li
            a#current(href=page.path)= page.name
        else
          li
            a(href=page.path)= page.name
    block content

    footer
        small QuizFacile 2016